{
  "name": "api-plugin-static",
  "version": "0.0.1",
  "description": "A static plugin for hapi using observables",
  "main": "lib/index.js",
  "scripts": {
    "build": "./node_modules/.bin/babel src --out-dir lib --presets es2015,stage-0",
    "start": "./node_modules/.bin/nodemon -w src --exec 'npm run build && node lib/index.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-core": "^6.5.2",
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0",
    "nodemon": "^1.9.1"
  },
  "dependencies": {
    "inert": "^3.2.0",
    "rxjs": "^5.0.0-beta.2"
  }
}
